/*
  Copy this file to "secrets.h" (same folder) and fill in your real values.
  secrets.h is gitignored -- it will never get committed or shared
  accidentally. drone_logger.ino includes "secrets.h", not this file.
*/

#ifndef SECRETS_H
#define SECRETS_H

#define WIFI_SSID     "YOUR_WIFI_NAME"
#define WIFI_PASSWORD "YOUR_WIFI_PASSWORD"
#define SERVER_URL    "http://192.168.1.50:8000/api/ingest/flight"  // your Pi/computer's IP
#define API_KEY       "PASTE_YOUR_INGEST_API_KEY_HERE"              // must match INGEST_API_KEY on the server
#define DRONE_NAME    "ESP32-Drone-1"

#endif
