# Copy this file to ".env" (same folder) and fill in your real value.
# .env is gitignored -- it will never get committed accidentally.

# Generate with: python3 -c "import secrets; print(secrets.token_hex(32))"
INGEST_API_KEY=paste-your-generated-key-here
